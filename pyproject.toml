[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "telegram-reset-bot"
version = "1.0.0"
requires-python = ">=3.10,<3.11"

dependencies = [
    "python-telegram-bot==20.3",
    "requests==2.31.0",
    "beautifulsoup4==4.12.2",
]
